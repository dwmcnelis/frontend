{{! app/templates/components/zz-list.hbs }}
{{#each result as |item|}}
	{{#if item.groupHeading}}
		{{#zz-list-heading}}
			{{item.heading}}
		{{/zz-list-heading}}
	{{else}}
		{{#zz-list-item}}
			{{yield item}}
		{{/zz-list-item}}
		{{/if}}


{{/each}}